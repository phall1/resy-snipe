// Package domain contains the core, dependency-free types that describe a
// snipe: Intent, SnipeState, SlotPayload union, Venue, BookingPolicy,
// ReleaseStrategy union, and Event. These types are shared between the
// engine, providers, and store and must not import any sibling internal/
// package.
package domain
