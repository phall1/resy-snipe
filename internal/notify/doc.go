// Package notify defines the Notifier interface and a stdout
// implementation. Phase 1 ships only stdout; SMS/iMessage/bot frontends
// are out of scope and will plug in later behind the same interface.
package notify
