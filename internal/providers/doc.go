// Package providers defines the cross-provider Provider interface and the
// sentinel error taxonomy the engine branches on. Concrete adapters
// (currently only internal/resy) implement this interface. No subpackages
// exist here until a second provider actually arrives.
package providers
